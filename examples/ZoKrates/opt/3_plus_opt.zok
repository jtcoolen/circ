//def main(private field x) -> field:
//    return x+x+x


// below fails for R1CS!!! :|
/*def main(private field x) -> field :
    field result = 0

    result = if (x % 2) == 0 then x * 3 else x + x + x fi

    return result*/
/*def main(private field x) -> field :
    //field result = 0

    //field result = 1 - 7 * x +9*x

    field result = if x % 3 == 0 then 3*x else -1 + 4*x fi
    //field result = if x % 3 == 1 then 3*x else 1 - 7 * x +9*x fi // fails

    return result*/

def main(private u32 x) -> u32 :
    //field result = 0

    //u32 result = x+x+x
   // u32 result = -x+x+3*x

  //  u32 result = -2*x+5*x
    //u32 result = 4*x-1
   // u32 result = 1  +9*x- 7 * x
    //u32 result = 1 - 7 * x +9*x
    //u32 result = 1+2*x
   // u32 result = -(2*x)+5*x
    //u32 result = 5*x-2*x
  // u32 result = 1*x-3*x+5*x // fails at is_satisfied


   u32 result = if true then 3 else 2 fi // (x%3)+2
    //field result = if x % 3 == 1 then 3*x else 1 - 7 * x +9*x fi // fails

    return result


/*def main(private field x) -> field :
    //field result = 0

    //field result = 1 - 7 * x +9*x
     //field result = 1 - 7 * x +9*x
    // field result = -x+4*x

    field result = if x % 3 == 0 then 3*x else -1 + 4*x fi
    //field result = if x % 3 == 1 then 3*x else 1 - 7 * x +9*x fi // fails

    return result*/