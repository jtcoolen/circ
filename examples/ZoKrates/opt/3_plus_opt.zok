//def main(private field x) -> field:
//    return x+x+x


// below fails for R1CS!!! :|
/*def main(private field x) -> field :
    field result = 0

    result = if (x % 2) == 0 then x * 3 else x + x + x fi

    return result*/
/*def main(private field x) -> field :
    //field result = 0

    //field result = 1 - 7 * x +9*x

    field result = if x % 3 == 0 then 3*x else -1 + 4*x fi
    //field result = if x % 3 == 1 then 3*x else 1 - 7 * x +9*x fi // fails

    return result*/

def main(private u32 x) -> u32 :
    //field result = 0

    //u32 result = x+x+x
    //u32 result = 1 - 7 * x +9*x
    //u32 result = 1+2*x
    //u32 result = 5*x-2*x
    u32 result = 1-3*x+5*x

    //u32 result = if x % 3 == 0 then 3*x else -1 + 4*x fi
    //field result = if x % 3 == 1 then 3*x else 1 - 7 * x +9*x fi // fails

    return result


/*def main(private field x) -> field :
    //field result = 0

    //field result = 1 - 7 * x +9*x

    field result = if x % 3 == 0 then 3*x else -1 + 4*x fi
    //field result = if x % 3 == 1 then 3*x else 1 - 7 * x +9*x fi // fails

    return result*/