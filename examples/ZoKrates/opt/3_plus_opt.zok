def main(private field x) -> field:
    return x+x+x


// below fails for R1CS!!! :|
/*def main(private field x) -> field :
    field result = 0

    result = if (x % 2) == 0 then x * 3 else x + x + x fi

    return result*/
/*def main(private field x) -> field :
    field result = 0

    //result = x + x + x

    result = if x % 3 == 0 then x else -x + 2*x-x+x*3 fi
    //result = if x % 3 == 1 then 3*x else x + 3*x fi // fails

    return result*/