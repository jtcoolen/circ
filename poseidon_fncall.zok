from "./poseidon.zok" import main as poseidon


def main(private field x) -> field :
    field y = PlonkMul2(x)

    field result = poseidon([x])
    field result2 = midnight_poseidon([result, y, x])

    return result2