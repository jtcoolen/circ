from "./poseidon.zok" import main as poseidon
struct Bar {
    u64 a
    field b
    field c
}

struct Pt {
    field x
    field y
}

def main(private Pt v) -> Bar :
    field z = PlonkMul2(v.x)

    field result = poseidon([v.y])
    field result2 = midnight_poseidon([result, z, v.x])

    u64 a = 6542
    u64 mod = 6

    return Bar { a: a%mod , b: result2, c:result}